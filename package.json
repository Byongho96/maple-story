{
  "name": "maple-story",
  "version": "1.0.0",
  "description": "",
  "main": "script.module.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && tsc-alias",
    "build:watch": "tsc && (concurrently \"tsc -w\" \"tsc-alias -w\")"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "types": "./script.module.d.ts",
  "devDependencies": {
    "tsc-alias": "^1.8.10",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
